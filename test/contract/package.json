{
    "name": "test",
    "version": "1.0.0",
    "scripts": {
        "build": "npx ncc build src/contract.js -o dist",
        "start": "npm run build && hpdevkit deploy dist"
    },
    "dependencies": {
        "everpocket-nodejs-contract": "file:../../dist",
        "hotpocket-nodejs-contract": "0.7.4"
    },
    "devDependencies": {
        "@vercel/ncc": "0.34.0"
    }
}
